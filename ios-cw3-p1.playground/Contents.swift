
var number1 = 4
var number2 = 10

// Add

func add(a:Int, b: Int) -> Int
{
    return a + b
}

var functionCall = add(a: number1, b: number2)
print("\(number1) + \(number2) = \(functionCall)")

//Multiplay

func multipaly(a:Int, b: Int) -> Int
{
    return a * b
}

var functionCallMultiplay = multipaly(a: number1, b: number2)
print("\(number1) * \(number2) = \(functionCall)")

// Divide

func divide (a:Int, b: Int) -> Int
{
    return a / b
}

var functionCalldivide = divide(a: number1, b: number2)
print("\(number1) / \(number2) = \(functionCall)")

// Subtract

func subtract(a:Int, b: Int) -> Int
{
    return a * b
}

var functionCallSubtract = subtract(a: number1, b: number2)
print("\(number1) - \(number2) = \(functionCall)")







